# GIOP fits -- LOC2a_66.89677N_162.57953W  ·  FLENS8_FOV08

Generated by `python make_giop_figures.py <this location folder>`, which reads `../FINAL_Rrs.csv` and the scans themselves. This file is REGENERATED on every run -- do not hand-edit it, edit the script instead.

Uses **our** port, `tmittal22/giop-workbench`, not upstream MATLAB.

**Read [`LOC2a_GIOP_FINDINGS.md`](LOC2a_GIOP_FINDINGS.md) first** -- the physics, the caveats, and what to quote for this station specifically.

## Figures

### `giop0_explainer.png` -- GIOP explained

What the inversion solves, what S_dg and eta are, original MATLAB vs this port.

### `giop1_band_choice.png` -- Band choice

6 bands vs 10/20 nm vs hyperspectral. GIOP is not a six-band algorithm -- it solves at whatever grid it is given.

### `giop2_seed_chain.png` -- The seed chain

Everything prescribed before any fitting happens: OC4 -> the Bricaud a*_phi SHAPE (not its amplitude), eta and S_dg from the same blue/green ratio.

### `giop3_fit_quality.png` -- Fit quality

Measured vs modelled r_rs and the residual, 6 bands vs hyperspectral.

### `giop4_conditioning.png` -- Conditioning

The measured shape uncertainty propagated through the inversion, both band choices -- six bands amplifies it far more than hyperspectral does.

### `giop5_sdg_assumption.png` -- S_dg swept

S_dg swept over its ordinary range, coloured by chi2 so a bad-fitting sweep arm cannot be mistaken for a real uncertainty.

### `giop6_all_fits.png` -- Every per-scan fit

GIOP on each individual angle-matched water/sky pair, constrained AND free shapes overlaid, so the misfit is not attributed to one bad mean spectrum.

### `giop7_covariance.png` -- Per-scan covariance

Every fitted parameter per scan, their cross-correlation, and which one absorbs a change in signal amplitude.

### `giop8_assumption_free.png` -- Assumption stripping

OC4 input, the a*_phi family, and freeing S_dg/eta, swept in turn -- each point coloured by its own chi2 so a rejected arm cannot be read as an uncertainty.

### `giop9_chi2_weighting.png` -- Chi-squared weighting

Does anything actually fit well? Ranks every arm by chi2 against the measured band uncertainty, maps chi2 over (S_dg, eta), and checks whether chi2 can be read as a likelihood here (it cannot -- the residual is smooth, not noise).

### `giop10_final_result.png` -- THE final result

Per-spectrum points, their mean, and the fit of the mean spectrum, for constrained / free / maximum-freedom. The self-consistency test (does M_phi reproduce its own seed) and the headline numbers to quote.

### `giop11_chi2_crossplot.png` -- Free vs constrained, per scan

Does the mean-spectrum improvement from freeing the shapes reproduce independently in every scan, or is it an averaging artefact?

## Headline numbers, this station (from `giop_FINAL.csv`)

chi2_nu is NOT comparable across stations with different measured shape uncertainty -- compare RMS misfit instead. See THEORY_GIOP_NOTE.md sect 4.

| config | M_phi | a_dg(443) m^-1 | b_bp(443) m^-1 | chi2_nu (nu=298) | RMS misfit |
|---|---|---|---|---|---|
| constrained | 8.74 | 0.7858 | 0.0742 | 221.8 | 8.2 % |
| free | 3.61 | 0.4906 | 0.04103 | 104.3 | 4.7 % |
| max freedom | 4.19 | 0.4868 | 0.04286 | 23.6 | 2.9 % |
